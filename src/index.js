// import "./styles.css";
import HashMap from "./hash.js";

const hashMap = new HashMap();

// hashMap.set("aweston", 1)
hashMap.set("aernsetb", 1)
hashMap.set("rstbrs", 1)

// if (index < 0 || index >= buckets.length) {
//   throw new Error("Trying to access index out of bounds");
// }
